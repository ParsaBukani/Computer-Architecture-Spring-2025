addi sp, zero, 0
addi tp, zero, 0
addi t0, zero, 20
addi t1, zero, 5

store_loop:
    sw t1, 0(sp)
    addi sp, sp, 4
    addi t1, t1, 3
    addi tp, tp, 1
    bne tp, t0, store_loop


addi a0, zero, 0
addi a1, zero, 0
addi a2, zero, 0
addi a3, zero, 20

loop:
  beq  a2, a3, done
  add  a4, a2, a2
  add  a4, a4, a4
  add  a5, a0, a4
  lw   a6, 0(a5)
  slt  a7, a1, a6
  beq  a7, zero, skip
  add  a1, a6, zero
skip:
  addi a2, a2, 1
  jal  zero, loop
done:

